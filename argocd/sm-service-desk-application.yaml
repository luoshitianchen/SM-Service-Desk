# ArgoCD Application: sm-service-desk（GitOps 持续交付）
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sm-service-desk
  namespace: argocd
  labels:
    app.kubernetes.io/name: sm-service-desk
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: sm-platform
  source:
    repoURL: https://github.com/luoshitianchen/sm-service-desk.git
    targetRevision: HEAD
    path: helm/sm-service-desk
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: sm-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
