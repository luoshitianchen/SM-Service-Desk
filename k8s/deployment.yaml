# Kubernetes Deployment: SM Service Desk
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sm-service-desk
  namespace: sm-platform
  labels:
    app: sm-service-desk
    app.kubernetes.io/version: "2.1.0"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: sm-service-desk
  template:
    metadata:
      labels:
        app: sm-service-desk
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8300"
    spec:
      serviceAccountName: sm-service-desk
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: sm-service-desk
          image: ghcr.io/luoshitianchen/sm-service-desk:${IMAGE_TAG:-latest}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8300
          envFrom:
            - configMapRef:
                name: sm-service-desk-config
            - secretRef:
                name: sm-service-desk-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: sm-service-desk
                topologyKey: kubernetes.io/hostname
