apiVersion: v1
kind: Service
metadata:
  name: sm-service-desk
  namespace: sm
  labels:
    app: sm-service-desk
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-service-desk
  ports:
  - name: http
    port: 8370
    targetPort: http
    protocol: TCP
